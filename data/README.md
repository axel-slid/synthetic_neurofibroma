# Data

Generated data, HSR assets, synthetic meshes, depth maps, images, and visualization outputs live here locally.

This directory is intentionally ignored for normal GitHub commits because it contains large files, including raw HSR meshes, texture maps, rendered notebooks, and generated arrays. Commit only small documentation/manifests here unless Git LFS or another artifact store is configured.
